beowolf = fopen('Beowolf.txt','r');
linesBeo = {};
while ~feof(beowolf)
  line = fget1(beowolf);
  linesBeo{end+1} = line;
end

fclose(beowolf);
wordsBeo = {}

for i = 1:length(linesBeo)
  temp = strsplit(linesBeo{i});
  wordsBeo = [wordsBeo,temp]
end

disp(words);
