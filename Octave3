function retval = getFreqTable(wordsIn, cellArrIn)
	for i = 1:length(wordsIn)
		for j = 1:length(cellArrIn)
			if wordsIn{i} == cellArrIn{j}
				cellArrIn{j, +1};
			else
				cellArrIn(wordsIn{i},end+1);
			endif
		end
	end
endfunction
